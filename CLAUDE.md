# 5DayDocs Source Repository

**This repo builds 5DayDocs. We use 5DayDocs to manage it (dogfooding).**

- `src/` = product (distributed to users)
- `docs/` = our tasks (real, not examples)

Read `README.md` for dev workflow. Read `DOCUMENTATION.md` for system rules.
